# TEMPORARY probe. Every 10 minutes - step syntax.
# Does nothing: no data, no Telegram, no commits.
name: probe-b
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:
jobs:
  tick:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "workflow=probe-b cron=*/10 * * * *"
          echo "fired $(date -u '+%F %T') UTC  trigger=${{ github.event_name }}"
